package com.usersystem.model;

/**
 * Enum que representa os papéis (perfis) de acesso dos usuários.
 * 
 * ROLE_USER  = usuário comum (acesso básico)
 * ROLE_ADMIN = administrador (acesso total)
 */
public enum Role {
    ROLE_USER,
    ROLE_ADMIN
}
